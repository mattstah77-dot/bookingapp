services:
  - name: bookingapp
    type: web
    env: node
    region: oregon
    buildCommand: npm install && npm run build && npm run build:server && ls -la dist/
    startCommand: node dist/server/index.js
    envVars:
      - key: NODE_ENV
        value: production
